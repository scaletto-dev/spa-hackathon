# ========================================
# Vercel Environment Variables
# ========================================
# These variables should be configured in Vercel Dashboard
# Settings → Environment Variables

# Backend API URL
VITE_API_URL=https://api.spa-hackathon.com

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Google Services (Optional)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Application Configuration
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0

# ========================================
# Vercel Deployment Notes
# ========================================
# 1. Go to Vercel Dashboard → Project → Settings → Environment Variables
# 2. Add each variable above with corresponding values
# 3. Set scope: Production, Preview, or Development
# 4. Variables will be injected at build time via build-output.js
# 5. Runtime env.js will be generated with these values
# 6. No rebuild needed when changing non-build-time variables
